<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:buttons="clr-namespace:AutoDock.Buttons"
    xmlns:layouts="clr-namespace:AutoDock.Layouts">
    <!-- 颜色开始 -->
    <SolidColorBrush x:Key="ButtonForeground" Color="#FFCACACA" />
    <SolidColorBrush x:Key="ButtonHoverForeground" Color="#FFFFFFFF" />
    <SolidColorBrush x:Key="ButtonLightForeground" Color="#FF333333" />
    <SolidColorBrush x:Key="ButtonBorderBrush" Color="Transparent" />
    <SolidColorBrush x:Key="ButtonHoverBorderBrush" Color="#FF515151" />
    <SolidColorBrush x:Key="ButtonBackground" Color="Transparent" />

    <Color x:Key="LayoutBackgroundColor">#FF515151</Color>
    <Color x:Key="LayoutLightBackgroundColor">#FFF78A09</Color>
    <Color x:Key="LayoutLightColor">#FFCCCCCC</Color>
    <Color x:Key="LayoutForegroundColor">#FF929292</Color>
    <Color x:Key="LayoutLightForegroundColor">#FF333333</Color>
    <SolidColorBrush x:Key="LayoutBackground" Color="{StaticResource LayoutBackgroundColor}" />
    <SolidColorBrush x:Key="LayoutLightBackground" Color="{StaticResource LayoutLightBackgroundColor}" />
    <SolidColorBrush x:Key="LayoutBorderBrush" Color="#FF515151" />
    <SolidColorBrush x:Key="LayoutLightBorderBrush" Color="#FFF78A09" />
    <SolidColorBrush x:Key="LayoutForeground" Color="{StaticResource LayoutForegroundColor}"/>
    <SolidColorBrush x:Key="LayoutLightForeground" Color="{StaticResource LayoutLightForegroundColor}"/>
    
    <SolidColorBrush x:Key="LayoutGroupItemBackground" Color="#FF515151" />
    <SolidColorBrush x:Key="LayoutGroupItemBorderBrush" Color="Transparent" />
    <SolidColorBrush x:Key="LayoutGroupSelectedItemBackground" Color="#FF333333" />
    <SolidColorBrush x:Key="LayoutGroupHoverItemBackground" Color="#FF666666" />
    <SolidColorBrush x:Key="LayoutGroupSelectedItemForeground" Color="#FFF78A09" />
    
    <Storyboard x:Key="LayoutGotFocusStoryboard">
        <ColorAnimation Storyboard.TargetProperty="Background.Color" To="{StaticResource LayoutLightBackgroundColor}" Duration="0:0:0"/>
        <ColorAnimation Storyboard.TargetProperty="Foreground.Color" To="{StaticResource LayoutLightForegroundColor}" Duration="0:0:0"/>
    </Storyboard>
    <Storyboard x:Key="LayoutLostFocusStoryboard">
        <ColorAnimation Storyboard.TargetProperty="Background.Color" To="{StaticResource LayoutBackgroundColor}" Duration="0:0:0"/>
        <ColorAnimation Storyboard.TargetProperty="Foreground.Color" To="{StaticResource LayoutForegroundColor}" Duration="0:0:0"/>
    </Storyboard>

    <Style x:Key="ButtonStyle" TargetType="Button">
        <Setter Property="Height" Value="16"/>
        <Setter Property="Width" Value="16"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="{StaticResource ButtonBorderBrush}"/>
        <Setter Property="Background" Value="{StaticResource ButtonBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource ButtonForeground}"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
    </Style>

    <Style TargetType="{x:Type buttons:WCloseButton}" BasedOn="{StaticResource ButtonStyle}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type buttons:WCloseButton}">
                    <Border
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Background="{TemplateBinding Background}">
                        <Path VerticalAlignment="Center"
                              HorizontalAlignment="Center"
                              Data="M0,0 L8,8 M0,8 L8,0"
                              Stretch="Fill"
                              Stroke="{TemplateBinding Foreground}"
                              StrokeThickness="1.5"
                              Height="9.5"
                              Width="9.5"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding Path=IsFocused, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type layouts:WDockContent}}}" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource ButtonLightForeground}"/>
                        </DataTrigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource ButtonHoverForeground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type buttons:WTopmostButton}" BasedOn="{StaticResource ButtonStyle}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type buttons:WTopmostButton}">
                    <Border
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Background="{TemplateBinding Background}">
                        <Path RenderOptions.EdgeMode="Aliased"
                              VerticalAlignment="Center"
                              HorizontalAlignment="Center"
                              Data="M1,0 L5,0 M1,-0.49319416 L1,6 M3,-0.49319416 L3,9 M4,-0.49319416 L4,6 M5,-0.49319416 L5,6 M0,6 L6.5,6"
                              Stretch="Fill"
                              Stroke="{TemplateBinding Foreground}"
                              UseLayoutRounding="False"
                              StrokeThickness="1"
                              Height="10.522"
                              Width="8"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding Path=IsFocused, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type layouts:WDockContent}}}" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource ButtonLightForeground}"/>
                        </DataTrigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource ButtonHoverForeground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style TargetType="{x:Type buttons:WDropDownButton}" BasedOn="{StaticResource ButtonStyle}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type buttons:WDropDownButton}">
                    <Border
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Background="{TemplateBinding Background}">
                        <Path VerticalAlignment="Center"
                              HorizontalAlignment="Center"
                              Data="M0,0 L8,0 4,4 Z"
                              Stretch="Fill"
                              Fill="{TemplateBinding Foreground}"
                              StrokeThickness="1"
                              Height="4"
                              Width="8"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding Path=IsFocused, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type layouts:WDockContent}}}" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource ButtonLightForeground}"/>
                        </DataTrigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource ButtonHoverForeground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style TargetType="{x:Type layouts:WDockContent}">
        <Setter Property="Background" Value="{StaticResource LayoutBackground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource LayoutBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1,0,1,1"/>
        <Setter Property="Foreground" Value="{StaticResource LayoutForeground}"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type layouts:WDockContent}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="20"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            RenderOptions.EdgeMode="Aliased"/>
                        <TextBlock
                            Margin="5,0"
                            Text="{TemplateBinding Title}"
                            VerticalAlignment="Center"
                            RenderOptions.EdgeMode="Aliased"/>
                        <StackPanel
                            VerticalAlignment="Center"
                            HorizontalAlignment="Right"
                            Orientation="Horizontal">
                            <buttons:WTopmostButton x:Name="TopmostButton"/>
                            <buttons:WCloseButton x:Name="CloseButton"/>
                        </StackPanel>
                        <Border
                            Grid.Row="1"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            RenderOptions.EdgeMode="Aliased">
                            <ContentPresenter/>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <EventTrigger RoutedEvent="GotFocus">
                            <BeginStoryboard Storyboard="{StaticResource LayoutGotFocusStoryboard}"/>
                        </EventTrigger>
                        <EventTrigger RoutedEvent="LostFocus">
                            <BeginStoryboard Storyboard="{StaticResource LayoutLostFocusStoryboard}"/>
                        </EventTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>        
    </Style>
    
    <!--<Style TargetType="{x:Type layouts:WLayoutGroupItem}">
        <Setter Property="Background" Value="{StaticResource LayoutGroupItemBackground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource LayoutGroupItemBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1,0,1,1"/>
        <Setter Property="Foreground" Value="{StaticResource LayoutForeground}"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="RenderOptions.EdgeMode" Value="Aliased"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type layouts:WLayoutGroupItem}">
                    <Border
                        Background="{TemplateBinding Background}">
                        <ContentPresenter
                            Margin="5"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            ContentSource="Header"
                            RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource LayoutGroupHoverItemBackground}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{StaticResource LayoutGroupSelectedItemBackground}"/>
                            <Setter Property="Foreground" Value="{StaticResource LayoutGroupSelectedItemForeground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->
    
    <!--<Style TargetType="{x:Type layouts:WLayoutGroup}">
        <Setter Property="Background" Value="{StaticResource LayoutBackground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource LayoutBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1,0,1,1"/>
        <Setter Property="Foreground" Value="{StaticResource LayoutForeground}"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type layouts:WLayoutGroup}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="20"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            RenderOptions.EdgeMode="Aliased"/>
                        <TextBlock
                            Margin="5,0"
                            VerticalAlignment="Center"
                            RenderOptions.EdgeMode="Aliased"/>
                        <StackPanel
                            VerticalAlignment="Center"
                            HorizontalAlignment="Right"
                            Orientation="Horizontal">
                            <buttons:TopmostButton x:Name="TopmostButton"/>
                            <buttons:CloseButton x:Name="CloseButton"/>
                        </StackPanel>
                        <Border
                            Grid.Row="1"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            RenderOptions.EdgeMode="Aliased">
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <EventTrigger RoutedEvent="GotFocus">
                            <BeginStoryboard Storyboard="{StaticResource LayoutGotFocusStoryboard}"/>
                        </EventTrigger>
                        <EventTrigger RoutedEvent="LostFocus">
                            <BeginStoryboard Storyboard="{StaticResource LayoutLostFocusStoryboard}"/>
                        </EventTrigger>
                    </ControlTemplate.Triggers>
                    <Grid x:Name="TemplateGrid">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="20"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Border
                            x:Name="TitleBackgroundBottom"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            RenderOptions.EdgeMode="Aliased"/>
                        <ContentControl
                            x:Name="TitleBottom"
                            Margin="5,0"
                            VerticalAlignment="Center"
                            RenderOptions.EdgeMode="Aliased"
                            Content="{Binding Path=SelectedItem.Header, RelativeSource={RelativeSource Mode=TemplatedParent}}"/>
                        <StackPanel
                            VerticalAlignment="Center"
                            HorizontalAlignment="Right"
                            Orientation="Horizontal">
                            <buttons:WTopmostButton/>
                            <buttons:WCloseButton/>
                        </StackPanel>
                        <Border
                            Grid.Row="1"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            RenderOptions.EdgeMode="Aliased">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <ContentPresenter
                                    x:Name="PART_SelectedContentHost"
                                    ContentSource="SelectedContent"
                                    VerticalAlignment="Stretch"
                                    HorizontalAlignment="Stretch"/>
                                <TabPanel
                                    x:Name="HeaderPanel"
                                    Canvas.ZIndex="1"
                                    Grid.Row="2"
                                    IsItemsHost="True"
                                    Background="{StaticResource LayoutBackground}"/>
                            </Grid>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->

    <Style TargetType="{x:Type layouts:WDockPanel}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type layouts:WDockPanel}">
                    <Grid>
                        <Grid x:Name="PART_Grid"/>
                        <Canvas x:Name="PART_Canvas"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type layouts:WDockedGroup}">
        <Setter Property="Background" Value="{StaticResource LayoutBackground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource LayoutBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1,0,1,1"/>
        <Setter Property="Foreground" Value="{StaticResource LayoutForeground}"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type layouts:WDockedGroup}">
                    <Grid x:Name="TemplateGrid">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="20"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Border
                            x:Name="TitleBackgroundBottom"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            RenderOptions.EdgeMode="Aliased"/>
                        <ContentControl
                            x:Name="TitleBottom"
                            Margin="5,0"
                            VerticalAlignment="Center"
                            RenderOptions.EdgeMode="Aliased"
                            Content="{Binding Path=SelectedItem.Header, RelativeSource={RelativeSource Mode=TemplatedParent}}"/>
                        <StackPanel
                            VerticalAlignment="Center"
                            HorizontalAlignment="Right"
                            Orientation="Horizontal">
                            <buttons:WTopmostButton/>
                            <buttons:WCloseButton/>
                        </StackPanel>
                        <Border
                            Grid.Row="1"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            RenderOptions.EdgeMode="Aliased">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <ContentPresenter
                                    x:Name="PART_SelectedContentHost"
                                    ContentSource="SelectedContent"
                                    VerticalAlignment="Stretch"
                                    HorizontalAlignment="Stretch"/>
                                <StackPanel
                                    x:Name="PART_Tab"
                                    Orientation="Horizontal">
                                </StackPanel>
                            </Grid>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>
